# =====================================================
# HRC-CEMS | Ingeniería Clínica - Hospital de Copiapó
# Dependencias del proyecto
# Instalar:  pip install -r requirements.txt
# =====================================================

# --- Núcleo web (app/) ---
Flask>=3.0
Jinja2>=3.1
Werkzeug>=3.0
waitress>=3.0                 # servidor WSGI para el despliegue LAN en Windows

# --- Configuración y entorno (core/) ---
python-dotenv>=1.0
pydantic>=2.6                 # validación de configuración y contratos de datos

# --- Base de datos (database/, core/) ---
SQLAlchemy>=2.0
# pyodbc>=5.1                 # descomentar si se usa SQL Server
# psycopg[binary]>=3.1        # descomentar si se usa PostgreSQL

# --- ETL: extracción (etl/extract/) ---
gspread>=6.0
google-auth>=2.28
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2
openpyxl>=3.1                 # .xlsx / .xlsm
requests>=2.31

# --- ETL: transformación (etl/transform/) ---
pandas>=2.2
numpy>=1.26
python-dateutil>=2.9
unidecode>=1.3                # normalización de texto (tildes, mayúsculas)

# --- Analítica y visualización (analytics/, reports/) ---
matplotlib>=3.8
scipy>=1.12
scikit-learn>=1.4             # clustering de OT y problemas de EEMM

# --- Calidad y pruebas (tests/) ---
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4                     # linter + formateador

# =====================================================
# OPCIONAL - NLP pesado (embeddings para clustering semántico).
# Se declara aparte porque son descargas de varios GB:
#   pip install -r requirements-ml.txt
# =====================================================
